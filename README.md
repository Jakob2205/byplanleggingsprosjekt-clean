# Planleggingsprosjekt

This is a planning project built with React and Vite. It uses Firebase for authentication and Firestore for data storage.

## Getting Started

1.  **Install dependencies:**
    ```bash
    npm install
    ```
2.  **Run the development server:**
    ```bash
    npm run dev
    ```
